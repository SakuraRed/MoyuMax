# language: zh-CN
@M28 @UI-THEME-002
功能: 自定义背景与纯数据主题包
  为了让界面符合个人喜好而不牺牲安全
  作为 MoyuMax 用户
  我希望用纯色、本地图片或纯数据主题包改变界面外观

  场景: 选择纯色背景
    假如 用户打开外观设置
    当 用户选择纯色背景并指定颜色
    那么 界面背景应立即应用该颜色
    而且 重启后保持

  场景: 选择本地图片背景
    假如 用户选择一张本地 PNG 图片
    当 应用为背景
    那么 界面应以压暗效果显示该图片
    当 窗口隐藏或减少动画开启
    那么 图片背景应降级为纯色底

  场景: 导入纯数据主题包
    假如 用户有一个合法主题包 JSON
    当 用户导入
    那么 配色应立即叠加到当前主题
    而且 主题包不得包含 CSS、脚本或远程资源

  场景: 拒绝恶意主题包
    假如 主题包包含 URL 或非颜色值
    当 用户导入
    那么 系统应拒绝并说明原因

  场景: 高对比忽略主题包
    假如 用户已应用主题包
    当 用户开启高对比
    那么 界面应忽略主题包配色以保证可读性

  @UI-THEME-002
  场景: 背景设置在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户设置背景与主题包
    那么 不应出现横向溢出、遮挡或文本贴边
