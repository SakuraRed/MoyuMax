# language: zh-CN
@M1 @UI-A11Y-001
功能: 首次运行设置
  为了无需理解启动器内部结构即可开始使用
  作为第一次启动 MoyuMax 的用户
  我希望完成最少的本地设置并在下次启动时保留选择

  @M1-FIRST-RUN-001
  场景: 新数据库显示首次运行引导
    假如 MoyuMax 使用一个新的本地状态数据库
    当 应用读取启动状态
    那么 应要求用户完成首次运行引导
    而且 默认关闭诊断遥测与 NAT 检测
    而且 更新只检查并提示，不自动下载或安装

  @M1-FIRST-RUN-002
  场景: 完成引导后跨重启保留选择
    假如 用户正在首次运行引导中
    当 用户确认语言、数据位置和隐私选择
    那么 所有选择应在同一数据库事务中保存
    当 MoyuMax 使用同一数据库再次启动
    那么 应直接进入首页
    而且 应返回与用户确认值一致的设置

  @M1-FIRST-RUN-003
  场景: 拒绝不受支持的网络数据位置
    假如 用户正在选择数据位置
    当 用户选择 UNC 或 SMB 网络路径并完成设置
    那么 系统应解释该位置不受支持
    而且 首次运行状态不得被标记为完成

  @M1-FIRST-RUN-004
  场景: 跳过引导时使用安全默认值
    假如 用户尚未完成首次运行引导
    当 用户选择跳过并使用默认值
    那么 系统应保存推荐数据位置
    而且 应关闭诊断遥测与 NAT 检测
    而且 应保持实例隔离开启

  @UI-A11Y-001
  场景: 文本与有边框的容器保持可读间距
    假如 用户正在 960×600 的首次运行引导中
    当 用户依次查看语言、数据位置、隐私和完成页面
    那么 卡片内容与卡片边缘之间应保留内边距
    而且 选项、设置和汇总文本不应贴住所在容器的边框
    而且 选项或设置行获得焦点时，文本与焦点框之间仍应保留至少 12 CSS 像素的水平间距
    而且 在 200% 放大时仍应保持该间距且不发生区域重叠
