# language: zh-CN
@M13 @UI-JAVA-002
功能: Java 环境管理与删除恢复闭环
  为了让高级用户掌控托管 Java 环境而不影响普通用户
  作为 MoyuMax 用户
  我希望查看、验证、指派、删除和恢复托管 Java 环境

  场景: 查看托管 Java 环境
    假如 存在一个已就绪的托管 Java 环境
    当 用户打开设置中的 Java 环境页面
    那么 应显示发行版、完整补丁版本、架构、位置、大小和健康状态
    而且 应显示正在引用该环境的实例

  @UI-JAVA-002
  场景: 删除仍被实例引用的环境
    假如 一个 Java 环境正被两个实例引用
    当 用户选择删除该环境
    那么 界面应列出两个受影响实例并提示不建议删除
    当 用户确认承担风险
    那么 系统应允许删除并保留可恢复的引用记录
    而且 不得删除实例本身或共享基础文件

  场景: 删除后启动受影响实例
    假如 一个实例引用的 Java 环境已被删除
    当 用户启动该实例
    那么 系统应明确说明环境已被删除
    而且 不得自动装回用户刚删除的构建
    而且 应指引用户恢复环境或为实例选择其他环境

  场景: 一键恢复被删除的环境
    假如 一个 Java 环境已被删除且实例仍引用它
    当 用户选择恢复该环境
    那么 系统应获取同一发行版与主版本线的最新可用补丁
    而且 恢复后实例引用应指向可用环境
    而且 实例可以正常启动

  场景: 设为实例环境需主版本一致
    假如 一个实例需要 Java 21
    当 用户尝试为其指派 Java 17 环境
    那么 系统应拒绝并说明主版本不一致
    当 用户为其指派兼容的 Java 21 环境
    那么 数据库与磁盘运行时清单应同步更新

  场景: 中断的删除在重启后收敛
    假如 一个环境被标记删除但文件清理未完成
    当 MoyuMax 重新启动
    那么 系统应继续完成清理且不触碰受管目录之外的文件
