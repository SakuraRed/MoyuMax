# language: zh-CN
@M14 @UI-TASK-001 @UI-TASK-002
功能: 任务控制完整化
  为了让用户精细控制下载与安装任务而不牺牲数据安全
  作为 MoyuMax 用户
  我希望暂停单个任务、调整优先级、限制全局速度，并让系统自动感知压力

  场景: 暂停单个任务
    假如 一个下载任务正在执行
    当 用户暂停该任务
    那么 任务应在分段边界中断并进入已暂停状态
    而且 其他任务与共享缓存不受影响
    当 用户恢复该任务
    那么 任务应从未完成内容继续且不重新下载已验证内容

  场景: 单任务暂停与全局暂停互不干扰
    假如 用户单独暂停了一个任务
    当 用户暂停全部任务再恢复全部任务
    那么 被单独暂停的任务应保持暂停
    而且 被全局暂停打断的任务应恢复执行

  场景: 优先级决定出队顺序
    假如 队列中有多个等待执行的任务
    当 用户提升其中一个任务的优先级
    那么 共享执行槽应先执行优先级更高的任务
    而且 优先级相同则按创建时间出队

  场景: 全局限速不被分段突破
    假如 用户设置了全局下载限速
    当 多个连接同时下载大文件分段
    那么 实测总吞吐不得系统性超过用户上限
    而且 最终哈希与原子提交不受影响

  场景: 网络劣化时自动收缩并发
    假如 下载器以多个连接工作
    当 单连接吞吐显著劣化或连续失败
    那么 系统应收缩有效连接数
    当 网络恢复稳定
    那么 有效连接数应缓慢回升

  场景: 任务详情展示控制状态
    假如 存在执行中的任务
    当 用户查看任务详情
    那么 应显示有效连接数、限速状态与降级原因
