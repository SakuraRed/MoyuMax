# language: zh-CN
@M25 @UI-UPDATE-002
功能: 启动器更新检查与安全下载
  为了让用户安全地获得新版本
  作为 MoyuMax 用户
  我希望手动检查更新、经校验下载安装包并自行安装

  场景: 已是最新版本
    假如 当前版本与最新发布一致
    当 用户检查更新
    那么 界面应说明已是最新且不产生下载

  场景: 发现新版本
    假如 发布频道有更高版本
    当 用户检查更新
    那么 界面应显示新版本号与发布说明
    而且 下载与安装入口需要用户手动触发

  场景: 下载经 SHA-256 校验
    假如 用户选择下载安装包
    当 下载完成
    那么 文件应通过摘要校验后才能保留
    当 校验失败
    那么 文件应被删除且提示校验失败

  场景: 跨越最低支持版本被阻止
    假如 新版本声明的最低可升级版本高于当前版本
    当 用户尝试下载
    那么 系统应阻止并说明需要先升级到中间版本

  场景: 卸载保留个人数据
    假如 用户卸载 MoyuMax
    当 卸载完成
    那么 实例、存档、备份、账户与 JDK 应原样保留

  @UI-UPDATE-002
  场景: 更新设置区在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户查看更新区
    那么 不应出现横向溢出、遮挡或文本贴边
