# language: zh-CN
@M24 @UI-CLI-001
功能: 内置 CLI（开发者模式）
  为了让高级用户以脚本方式查询和控制 MoyuMax
  作为开发者
  我希望在显式开启后使用稳定的命令行接口

  场景: 默认关闭时拒绝命令
    假如 CLI 从未被启用
    当 用户执行任何 CLI 命令
    那么 应返回未启用指引且退出码为 4

  场景: 开启后读取实例清单
    假如 用户已在开发者设置中开启 CLI
    当 用户执行 instances list
    那么 应输出版本化 JSON 实例清单且退出码为 0

  场景: 全局暂停写操作支持 dry-run
    假如 CLI 已开启且任务正在运行
    当 用户执行 tasks pause-all --dry-run
    那么 应输出将要执行的动作
    而且 任务实际不应被暂停
    当 用户执行 tasks pause-all
    那么 全部任务应进入全局暂停

  场景: 立即创建备份
    假如 CLI 已开启且实例包含世界
    当 用户执行 backups create --instance <id>
    那么 应创建一个全量备份且输出其标识

  场景: 未知命令返回用法错误
    假如 CLI 已开启
    当 用户执行不存在的命令
    那么 应返回用法说明且退出码为 2

  @UI-CLI-001
  场景: 开发者设置区在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户查看开发者区
    那么 不应出现横向溢出、遮挡或文本贴边
