# language: zh-CN
@M9 @UI-TRAY-001
功能: 托盘常驻、关闭窗口选择与快速唤醒
  为了让 MoyuMax 作为安装式常驻应用低占用待命
  作为 MoyuMax 用户
  我希望关闭窗口时能选择最小化到托盘或退出，并能从托盘快速恢复和退出

  场景: 首次关闭窗口询问最小化或退出
    假如 用户从未选择过关闭窗口行为
    当 用户关闭 MoyuMax 主窗口
    那么 系统应询问最小化到系统托盘还是退出 MoyuMax
    而且 用户可以选择记住本次选择
    而且 记住选择的复选框默认不勾选

  场景: 记住最小化选择后再次关闭不再询问
    假如 用户已记住关闭窗口时最小化到托盘
    当 用户关闭 MoyuMax 主窗口
    那么 系统不再弹出询问
    而且 窗口直接最小化到系统托盘
    而且 托盘菜单仍提供明确的退出 MoyuMax 操作

  @UI-TRAY-002
  场景: 最小化到托盘后唤醒恢复上次页面
    假如 用户上次停留在任务中心并滚动到队列中部
    当 用户最小化到系统托盘
    那么 系统应销毁窗口界面并只保留后台核心与托盘
    当 用户从托盘恢复 MoyuMax
    那么 系统应重新打开主窗口并回到任务中心
    而且 主内容滚动位置应恢复到队列中部

  @UI-TRAY-003
  场景: 托盘菜单展示统一事实
    假如 存在两个实例且其中一个正在运行
    而且 存在两个进行中的任务
    当 用户打开托盘菜单
    那么 菜单应提供显示 MoyuMax 操作
    而且 菜单应列出最近实例快速启动并标记运行中的实例
    而且 菜单应以只读形式展示活动任务摘要
    而且 菜单应提供暂停全部任务和退出 MoyuMax 操作

  @UI-TRAY-004
  场景: 游戏运行中退出前说明影响
    假如 一个实例的游戏正在运行
    而且 存在一个活动下载任务
    当 用户选择退出 MoyuMax
    那么 系统应说明退出将安全终止正在运行的游戏
    而且 系统应说明存档会在终止前完成一次退出备份
    而且 系统应说明活动下载会转为可在下次启动时恢复的暂停状态
    当 用户确认退出
    那么 系统应先请求安全终止游戏并等待退出后备份完成
    而且 活动下载任务应进入可恢复的暂停状态而不是失败状态
    而且 已提交的共享文件与实例数据不得被修改

  @UI-TASK-002
  场景: 暂停全部任务后从未完成分段恢复
    假如 一个下载任务已经完成部分文件分段
    当 用户选择暂停全部任务
    那么 系统应停止新的任务调度
    而且 执行中的下载应在分段边界中断并保留已写入的暂存内容
    当 用户选择恢复全部任务
    那么 暂停的任务应重新入队
    而且 系统应先校验可续传内容再继续下载
    而且 暂停状态应在应用重启后保持

  @UI-TRAY-006
  场景: 唤醒恢复时敏感状态重新遮罩
    假如 一个页面被标记为敏感明文页面
    当 用户从系统托盘恢复 MoyuMax
    那么 系统不应恢复该页面的明文状态
    而且 系统应回到首页
    而且 任何弹窗或凭据输入状态都不应自动重开

  @UI-TRAY-007
  场景: 托盘初始化不阻塞首个可交互窗口
    假如 本地已有一个可启动实例
    当 用户启动 MoyuMax
    那么 首个可交互窗口不应等待托盘图标与后台任务检查完成
    而且 用户可以立即启动本地实例

  @UI-TRAY-005
  场景: 托盘唤醒与后台内存满足性能预算
    假如 MoyuMax 已最小化到系统托盘
    当 用户从托盘恢复 MoyuMax
    那么 从触发到界面可操作的 P95 耗时不应超过 250 毫秒
    而且 窗口销毁后的后台私有内存目标不超过 80 MiB 且硬上限不超过 120 MiB
