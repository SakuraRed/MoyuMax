# language: zh-CN
@M4 @UI-HOME-001 @UI-LAUNCH-001 @UI-LOG-001
功能: 从已安装实例启动 Minecraft
  为了让用户无需手工编写 Java 命令即可进入游戏
  作为已经安装一个实例的用户
  我希望 MoyuMax 校验环境、展开官方参数并管理游戏进程与日志

  @M4-LAUNCH-001
  场景: 使用完全隔离实例和本地离线身份启动
    假如 一个 ready 实例具有托管 Java、共享 classpath、资源索引和版本化运行时清单
    当 用户以本地离线身份启动该实例
    那么 系统应合并 Minecraft 与加载器的 JVM 和游戏参数
    而且 所有路径应指向受管共享存储或该实例的隔离目录
    而且 命令中不得残留未解析占位符

  @M4-LAUNCH-002
  场景: 启动前发现关键文件缺失
    假如 ready 实例的 Java、classpath、资源索引或日志配置缺失
    当 用户尝试启动实例
    那么 系统应在创建游戏进程前停止
    而且 应返回可定位到缺失项目的错误

  @M4-LAUNCH-003
  场景: 游戏进程输出与退出状态被持久化
    假如 实例通过启动前完整性检查
    当 游戏进程写入标准输出与标准错误后退出
    那么 两路输出应保存到实例日志目录
    而且 启动会话应记录开始时间、结束时间和真实退出码

  @M4-LAUNCH-004
  场景: 游戏异常退出可供诊断
    假如 游戏进程已经成功创建
    当 游戏以非零状态退出
    那么 启动会话应标记为失败而不是仍在运行
    而且 日志路径和退出码应可供后续崩溃报告使用

  @M4-LAUNCH-005
  场景: 同一实例已经在运行
    假如 一个实例已有运行中的游戏会话
    当 用户再次请求启动同一实例
    那么 系统不应创建第二个游戏进程
    而且 应提示该实例已经在运行

  @M4-LAUNCH-006
  场景: 用户主动停止正在运行的游戏
    假如 一个实例已有运行中的游戏会话
    当 用户请求停止该实例
    那么 系统应结束对应的游戏进程
    而且 启动会话应标记为已停止并保留已有日志

  @M4-LAUNCH-007
  场景: 启动器重启后恢复遗留会话状态
    假如 启动器退出时仍有处于启动中或运行中的会话
    当 MoyuMax 再次打开同一状态数据库
    那么 遗留会话应标记为已中断
    而且 应记录结束时间和可供诊断的中断原因

  @M4-LAUNCH-008
  场景: 启动诊断不会暴露完整命令
    假如 启动参数包含账户和本地路径上下文
    当 系统输出启动计划的调试摘要
    那么 摘要不得包含完整参数列表或访问令牌
    而且 会话记录只保存诊断所需的状态和日志路径

  @M4-LAUNCH-009
  场景: 只有显式停止请求才标记游戏已停止
    假如 游戏会话的停止控制器被释放但没有发出停止请求
    当 游戏进程随后自行退出
    那么 会话应按真实退出码标记完成或失败
    而且 不得错误标记为用户主动停止

  @UI-LAUNCH-001
  场景: 桌面启动命令不会阻塞界面
    假如 首页显示一个可以启动的 ready 实例
    当 用户点击启动游戏
    那么 桌面命令应立即返回启动中的会话
    而且 游戏进程应由后台协调器持续监控
    而且 用户可以在同一实例卡片上请求停止

  @M4-LAUNCH-010
  场景: 运行时清单不得绕过受管环境索引
    假如 磁盘与数据库中的运行时清单都被改为引用未登记的 Java 或共享目录
    当 用户尝试启动该实例
    那么 系统应在创建游戏进程前停止
    而且 应提示运行时清单与受管环境索引不一致

  @M4-LAUNCH-011
  场景: 日志初始化失败不会留下假运行会话
    假如 启动会话已经预留但实例日志目录无法创建
    当 后台协调器尝试创建游戏进程
    那么 启动应失败且不得创建游戏进程
    而且 会话应标记为失败并记录可诊断原因
    而且 用户修复目录问题后可以再次启动该实例
