# language: zh-CN
@M7 @UI-RECYCLE-001 @DATA-SAFETY-001
功能: 实例内置回收站
  为了避免误删实例后丢失存档和配置
  作为 MoyuMax 用户
  我希望删除实例时先进入完全本地的回收站，并能在保留期内恢复

  场景: 删除实例后可完整恢复
    假如 一个可启动实例使用托管 Java 并包含本地存档
    当 用户确认将该实例移入回收站
    那么 实例应从可启动列表隐藏
    而且 实例目录应移动到 MoyuMax 受管回收站
    而且 回收站应记录原位置、占用空间和 30 天保留期限
    而且 托管 Java 不得被删除或重复安装
    当 用户在原位置未被占用时恢复该实例
    那么 实例、存档和关联索引应回到删除前状态

  场景: 正在运行的实例不能删除
    假如 一个实例的游戏进程仍在运行
    当 用户尝试将该实例移入回收站
    那么 操作应被拒绝并说明需要先停止游戏
    而且 实例目录和数据库索引不得改变

  场景: 恢复位置冲突时不覆盖现有内容
    假如 一个实例已经进入回收站
    而且 原位置后来出现了其他文件
    当 用户尝试恢复该实例
    那么 操作应被拒绝并说明原位置已被占用
    而且 现有文件与回收站内容均不得被覆盖

  场景: 用户明确确认永久删除
    假如 回收站中存在一个实例
    当 界面展示实例名称、对象数量、占用空间和不可恢复说明
    而且 用户确认永久删除
    那么 回收站文件和实例索引应被删除
    而且 托管 Java 与共享基础文件不得被删除

  场景: 回收操作中断后自动收敛
    假如 实例回收操作已经持久化但文件移动尚未完成
    当 MoyuMax 重新启动
    那么 系统应完成原定的回收操作
    而且 不应同时留下两个可管理实例副本
