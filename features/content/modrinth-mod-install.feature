# language: zh-CN
@M5 @UI-MOD-001 @UI-OFFLINE-001
功能: 从 Modrinth 为实例安装模组及依赖
  为了无需手工寻找兼容文件和依赖即可扩展游戏
  作为拥有一个 Fabric 实例的用户
  我希望搜索兼容模组、确认依赖闭包并原子安装到指定实例

  @M5-SEARCH-001
  场景: 搜索结果严格匹配目标实例
    假如 用户选择一个 Minecraft 26.2 Fabric 客户端实例
    当 用户在 Modrinth 搜索模组
    那么 请求应限定项目类型、游戏版本、Fabric 和客户端兼容性
    而且 结果应保存不可变项目 ID 而不是只保存 slug
    而且 界面不得加载远程项目图片

  @M5-DEPENDENCY-001
  场景: 必需依赖自动加入而可选依赖等待选择
    假如 用户选择的模组有一个必需依赖和一个可选依赖
    当 系统生成安装确认计划
    那么 必需依赖应自动加入同一事务
    而且 可选依赖应显示原因并等待用户选择
    而且 未经用户确认不得安装可选依赖

  @M5-DEPENDENCY-002
  场景: 依赖闭包按实例兼容性解析并去重
    假如 多层依赖重复引用同一项目并形成回边
    当 系统解析完整依赖闭包
    那么 每个项目只应选择一个兼容版本
    而且 指定 version ID 的依赖仍应核对目标实例兼容性
    而且 安装顺序应稳定且依赖位于引用者之前

  @M5-DEPENDENCY-003
  场景: 无法识别或不兼容的依赖阻止安装
    假如 必需依赖只有文件名而没有项目或版本 ID
    或者 指定版本不支持目标游戏版本或加载器
    当 系统生成安装计划
    那么 系统不得猜测或静默忽略依赖
    而且 应说明具体依赖和不兼容原因

  @M5-INSTALL-001
  场景: 模组与必需依赖作为一个事务安装
    假如 安装计划包含目标模组和全部必需依赖
    当 统一任务队列执行该计划
    那么 所有文件应先下载到任务暂存区
    而且 每个文件应通过大小、SHA-1 和 SHA-512 校验
    而且 全部就绪后才可发布到实例 mods 目录并写入数据库索引
    而且 成功后不应留下任务暂存文件

  @M5-INSTALL-002
  场景: 文件冲突不会覆盖用户内容
    假如 实例 mods 目录已有同名但哈希不同的文件
    当 用户尝试安装 Modrinth 版本
    那么 任务应失败并列出冲突文件
    而且 原文件和其他已安装模组不得被修改

  @M5-INSTALL-003
  场景: 中途失败不会留下部分依赖
    假如 多文件事务在下载、校验、文件提交或数据库提交时失败
    当 任务进入可恢复失败
    那么 本次新增到 mods 目录的文件应全部撤销
    而且 已有文件和世界存档不得被修改
    而且 用户应能从统一任务中心重试未完成内容

  @M5-OFFLINE-001
  场景: 离线时已安装内容仍可管理和启动
    假如 Modrinth API 和外网均不可用
    而且 实例已经安装并索引模组
    当 用户打开实例内容页或启动游戏
    那么 本地模组列表和启动能力应保持可用
    而且 远程搜索应明确显示离线而不是本地故障
