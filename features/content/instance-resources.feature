# language: zh-CN
@M16 @UI-RES-001 @UI-RES-002
功能: 实例资源内容管理（资源包/光影/数据包）
  为了让用户统一管理实例的非模组资源内容
  作为 MoyuMax 用户
  我希望导入本地资源包、光影和数据包并控制启用状态

  场景: 导入资源包进入实例
    假如 一个实例处于可启动状态
    当 用户选择一个本地资源包 zip 文件导入
    那么 文件应原子进入实例 resourcepacks 目录
    而且 清单应显示该资源包为启用状态

  场景: 同名文件拒绝导入且不覆盖
    假如 实例 resourcepacks 目录已有同名文件
    当 用户导入同名资源包
    那么 系统应拒绝导入
    而且 既有文件内容保持不变

  场景: 导入失败不留半成品
    假如 用户导入一个光影包
    当 索引写入失败
    那么 实例目录不应留下该文件
    而且 清单中不应出现该项

  场景: 启用与停用切换
    假如 实例已导入一个资源包
    当 用户切换为停用
    那么 文件名应以 .disabled 结尾且游戏不再加载它
    当 用户重新启用
    那么 文件名应恢复原样

  场景: 数据包必须装入用户选择的世界
    假如 实例存在两个世界
    当 用户导入数据包并选择世界乙
    那么 文件应只进入世界乙的 datapacks 目录
    而且 未选择世界时不得导入数据包

  场景: 实例隔离
    假如 存在两个实例
    当 用户向实例甲导入资源包
    那么 实例乙的目录与清单不应出现该资源包

  @UI-RES-001
  场景: 资源内容区按类型分段展示
    假如 实例已有资源包和数据包
    当 用户打开资源中心
    那么 界面应按模组、资源包、光影、数据包分段
    而且 每项显示启用开关

  @UI-RES-002
  场景: 资源内容区在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户浏览资源内容区
    那么 不应出现横向溢出、遮挡或文本贴边
