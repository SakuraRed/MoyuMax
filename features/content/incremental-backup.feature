# language: zh-CN
@M19 @UI-BACKUP-002
功能: 运行期间增量备份与世界历史
  为了让长时间游戏会话的进度也有可回滚的历史
  作为 MoyuMax 用户
  我希望游戏运行期间自动产生不占空间的增量备份

  场景: 游戏运行期间定时产生增量备份
    假如 一个实例正在运行游戏且定时备份间隔为 1 分钟
    当 超过一个间隔且 saves 发生变化
    那么 系统应创建只包含变化文件的增量备份
    而且 备份清单记录相对上次备份删除的文件

  场景: 没有可用基准时回退为全量
    假如 实例没有任何带文件索引的备份
    当 到达定时备份时刻
    那么 系统应创建全量备份而不是失败的增量

  场景: 回滚到增量点逐文件一致
    假如 实例有全量备份和其后两个增量备份
    当 用户回滚到第二个增量点
    那么 saves 应与该时点的状态逐文件一致
    而且 增量之后删除的文件不应重现

  场景: 保留清理不破坏恢复链
    假如 保留数量为 2 且实例存在一串全量与增量备份
    当 触发清理
    那么 超出的最旧备份及其增量链段应一起移除
    而且 不得留下基准缺失的增量备份

  场景: 会话结束后不再产生定时备份
    假如 游戏会话已结束
    当 超过下一个间隔
    那么 系统不应再创建新的定时备份

  @UI-BACKUP-002
  场景: 备份设置与时间线在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户调整备份间隔与保留数量并浏览备份时间线
    那么 不应出现横向溢出、遮挡或文本贴边
