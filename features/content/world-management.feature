# language: zh-CN
@M17 @UI-WORLD-001
功能: 世界存档管理
  为了让用户安全地管理实例世界
  作为 MoyuMax 用户
  我希望查看世界清单、导入导出世界并回滚到备份

  场景: 查看实例世界清单
    假如 一个实例包含两个世界
    当 用户打开世界存档区
    那么 应显示两个世界的名称、占用和最近游玩时间

  场景: 导出后可再导入
    假如 实例存在一个世界
    当 用户导出该世界到本地 ZIP
    那么 ZIP 应包含完整世界目录
    当 用户把该 ZIP 导入另一个实例
    那么 世界应原子出现在目标实例 saves 中

  场景: 同名世界拒绝导入
    假如 实例 saves 已有同名世界
    当 用户导入同名世界 ZIP
    那么 系统应拒绝且不修改既有世界

  场景: 回滚先创建恢复点
    假如 实例有一个已完成备份且当前 saves 已变化
    当 用户确认回滚到该备份
    那么 系统应先创建恢复点备份
    而且 saves 应恢复到备份时状态

  场景: 恢复点创建失败时中止回滚
    假如 恢复点备份无法创建
    当 用户确认回滚
    那么 系统应中止回滚且 saves 保持不变

  场景: 回滚交换中断后重启收敛
    假如 回滚交换进行到一半应用退出
    当 MoyuMax 重新启动
    那么 saves 应完整可用且不存在两侧目录同时丢失

  @UI-WORLD-001
  场景: 世界存档区在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户浏览世界存档区并打开回滚确认
    那么 不应出现横向溢出、遮挡或文本贴边
