# language: zh-CN
@M15 @UI-MOD-001 @UI-UPDATE-001
功能: Modrinth 全加载器与按实例更新策略
  为了让 Quilt、Forge、NeoForge 实例获得一致的模组生态体验
  作为 MoyuMax 用户
  我希望为任何受支持加载器的实例安装 Modrinth 模组并安全更新

  场景: 为 Quilt 或 Forge 实例安装模组
    假如 一个 Quilt 实例处于可启动状态
    当 用户为该实例安装一个 Modrinth 模组
    那么 模组及其必需依赖应进入同一原子事务
    而且 失败时实例应恢复到安装前状态

  @UI-UPDATE-001
  场景: 默认关闭时只提示不下载
    假如 用户没有为该实例开启自动更新
    当 系统发现兼容更新
    那么 系统只能提示更新可用
    而且 不得下载或修改实例内容

  场景: 更新前创建恢复点并可完整回滚
    假如 实例已安装一个模组且有可用更新
    当 用户确认更新
    那么 系统应先把旧文件移入恢复点再发布新文件
    当 更新事务失败
    那么 实例应恢复到更新前状态且不留半成品

  场景: 更新中断后重启自动恢复旧文件
    假如 更新替换进行到一半应用退出
    当 MoyuMax 重新启动
    那么 系统应先把恢复点中的旧文件放回实例
    而且 任务进入恢复确认而不是伪装完成

  场景: 按实例开启自动更新
    假如 用户为实例开启自动更新
    当 系统发现兼容更新
    那么 界面应提供用户明确触发的全部更新入口
    而且 未经用户触发仍不得修改实例
