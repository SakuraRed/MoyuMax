# language: zh-CN
@M10 @UI-SOURCE-001 @UI-DOWNLOAD-001
功能: 下载来源统一与大文件多线程分段下载
  为了让用户在不可靠网络下快速且可信地完成安装
  作为 MoyuMax 用户
  我希望系统自动选择合适的下载来源，并在来源支持时分段加速大文件下载

  @UI-SOURCE-001
  场景: 默认镜像优先路由到内置镜像
    假如 用户没有修改下载来源策略
    当 系统准备下载 Modrinth 模组或 Minecraft 游戏文件
    那么 Modrinth 与 CurseForge 文件请求应路由到 MCI Mirror
    而且 Minecraft 游戏文件请求应按 BMCLAPI 文档路由到内置镜像
    而且 任务详情应记录真实来源

  @UI-SOURCE-001
  场景: 官方优先模式下官方源失败可静默回退镜像
    假如 用户选择“官方源优先”
    当 官方源下载失败
    那么 任务可以静默切换到内置可信镜像
    而且 切换记录应可在任务详情查看

  @UI-SOURCE-002
  场景: 自定义源失败不切换
    假如 用户选择了自定义源
    当 自定义源下载失败
    那么 系统不得切换到官方源或内置镜像
    而且 应提示用户测试、重试或更改来源

  @UI-SOURCE-003
  场景: CurseForge 官方源不可用
    假如 用户选择“官方源优先”
    当 用户准备下载 CurseForge 文件
    那么 系统应明确提示 CurseForge 官方源不可用
    而且 不得发起官方直连
    而且 应提供通过 MCI Mirror 下载的路径且不得标记为官方直连

  @UI-DOWNLOAD-001
  场景: 支持 Range 的大文件分段并行下载
    假如 一个大文件具有可信大小和校验值
    而且 当前来源支持 HTTP Range
    当 任务执行下载
    那么 系统应在统一连接预算内并行下载不重叠分段
    而且 分段清单应记录来源、范围与已完成长度
    当 所有分段完成
    那么 系统应合并并验证完整文件的大小和可信哈希
    而且 只有验证通过的文件才能原子提交

  @UI-DOWNLOAD-001
  场景: 分段损坏时精准重试
    假如 一个大文件的部分分段已经下载完成
    当 其中一个分段的内容与清单不一致
    那么 系统只重新下载损坏的分段
    而且 已验证分段不得重新下载
    当 最终合并校验失败
    那么 文件不得进入共享存储或实例

  @UI-DOWNLOAD-001
  场景: 来源忽略 Range 时安全降级
    假如 来源对分段请求返回完整响应或忽略 Range
    当 多线程下载无法安全继续
    那么 系统应降级为单连接续传或清理对应内容后重新下载
    而且 不得删除其他已验证分段、共享文件或实例数据

  @UI-DOWNLOAD-001
  场景: 远端对象证据变化时废弃旧分段
    假如 一个大文件的已有分段记录了 ETag 或 Last-Modified
    当 来源返回的对象证据与清单不一致
    那么 系统应废弃对应旧分段并重新下载
    而且 不得在旧证据之上追加新内容

  @UI-TASK-002
  场景: 暂停与重启后复用有效分段
    假如 一个分段下载任务已经完成部分分段
    当 用户暂停全部任务或应用重启
    那么 已完成的有效分段应保留在任务暂存区
    当 用户恢复全部任务
    那么 系统应先校验分段清单再继续未完成分段
    而且 恢复过程不自动联网下载已验证内容

  @UI-DOWNLOAD-002
  场景: 两个任务共享连接预算时保持公平
    假如 两个任务同时下载大文件
    当 全局连接预算被占满
    那么 两个任务都应能持续取得进展
    而且 任何单一文件的分段数不得超过有界上限

  @UI-OFFLINE-001
  场景: 镜像离线不影响本地功能
    假如 内置镜像与官方源都不可用
    当 用户打开 MoyuMax
    那么 本地实例、内容索引、备份与回收站应保持可用
    而且 界面应区分离线不可用的远程操作与本地故障
