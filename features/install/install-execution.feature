# language: zh-CN
@M3 @UI-INSTALL-001 @UI-INSTALL-002 @UI-TASK-001
功能: 校验下载并原子提交 Minecraft 实例
  为了让安装中断、来源异常或损坏文件不会破坏现有游戏
  作为正在安装第一个游戏的用户
  我希望所有内容先进入任务暂存区，校验成功后再提交

  @M3-EXECUTE-001
  场景: 推荐版本的完整计划执行成功
    假如 一个安装任务包含游戏、资源、加载器与托管 Java 的可信下载清单
    当 任务执行器完成下载、校验、环境安装和加载器处理
    那么 所有可共享文件应提交到全局受管存储
    而且 实例可变目录应保持完全隔离
    而且 实例只能在最后一次数据库事务成功后变为可启动
    而且 任务阶段应依次记录到“创建回滚点”并标记完成

  @M3-EXECUTE-002
  场景: 从已校验的部分内容继续下载
    假如 任务暂存区中存在一个未完成文件
    而且 来源支持 HTTP Range
    当 用户恢复任务
    那么 执行器应从现有长度继续请求
    而且 完成后应校验完整大小与可信哈希

  @M3-EXECUTE-003
  场景: 来源不支持续传时重新下载对应文件
    假如 任务暂存区中存在一个未完成文件
    而且 来源忽略 HTTP Range 并返回完整内容
    当 用户恢复任务
    那么 执行器应清空该文件的未完成内容
    而且 应从头写入并重新校验，不得把两份内容拼接

  @M3-EXECUTE-004
  场景: 下载内容哈希不匹配
    假如 来源返回的文件与计划中的可信哈希不一致
    当 执行器验证文件
    那么 任务应失败并显示可恢复原因
    而且 已有共享文件和实例不得被替换
    而且 错误文件应保留在任务暂存区供重试时清理

  @M3-EXECUTE-005
  场景: Java 压缩包试图写出受管目录
    假如 托管 Java 压缩包包含绝对路径、父目录穿越或符号链接条目
    当 执行器解包游戏环境
    那么 应拒绝整个环境并标记任务失败
    而且 受管 Java 目录之外不得产生文件

  @M3-EXECUTE-006
  场景: 异常退出后继续同一安装计划
    假如 运行中任务在提交前异常退出
    当 用户确认继续
    那么 执行器应复用已经通过哈希校验的共享文件和暂存文件
    而且 只下载缺失或校验失败的内容
    而且 不得自动改变用户已经确认的计划快照

  @M3-EXECUTE-007
  场景: Java 元数据大小滞后时校准下载快照
    假如 Azul API 提供了托管 Java 的可信 SHA-256
    而且 API 声明的大小与下载端当前 Content-Length 不一致
    当 系统生成用户确认的安装计划快照
    那么 Java 下载项应记录下载端当前 Content-Length
    而且 完成下载后仍应按 Azul 提供的 SHA-256 校验内容

  @M3-EXECUTE-008
  场景: 新版元数据将原生库作为独立 Maven 分类器
    假如 Minecraft 元数据分别列出 Windows x64、x86 和 ARM64 原生库 artifact
    当 Windows x64 安装执行器准备实例运行时
    那么 系统应只下载 Windows x64 原生库
    而且 新版原生 JAR 应保留在 classpath 供运行时按官方 JVM 参数自解包
    而且 旧版 natives/classifiers 原生库仍应由启动器安全预解包
