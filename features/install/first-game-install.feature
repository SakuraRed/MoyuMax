# language: zh-CN
@M2 @UI-INSTALL-001 @UI-INSTALL-002 @UI-JAVA-001 @UI-TASK-001
功能: 安装第一个游戏
  为了让没有 Java 与启动器经验的新用户也能得到可恢复的游戏实例
  作为首次安装 Minecraft 的用户
  我希望接受推荐配置后由 MoyuMax 统一规划并持久化安装任务

  @M2-INSTALL-001
  场景: 默认配置生成游戏与托管 Java 的同一安装任务
    假如 本地没有任何实例和兼容的托管 Java
    而且 官方元数据中的推荐稳定版需要 Java 21
    当 用户接受推荐版本、Fabric 加载器、默认名称和完全隔离
    那么 系统应创建一个持久化安装任务
    而且 任务应包含游戏下载、文件验证、安装游戏环境、应用加载器和提交更改阶段
    而且 Java 应安装在 MoyuMax 管理的数据位置，不修改系统 Java

  @M2-INSTALL-002
  场景: 相同完整 Java 构建只复用一次
    假如 已托管 Azul Zulu 21 的指定完整构建和 x64 架构
    当 用户创建另一个需要同一构建的安装任务
    那么 安装计划应引用现有 Java 环境
    而且 不应再次安排 Java 下载或解包

  @M2-INSTALL-003
  场景: 异常退出后的运行中任务等待用户决定
    假如 一个安装任务已经进入运行状态但尚未提交
    当 MoyuMax 使用同一状态数据库重新启动
    那么 任务应变为“等待恢复确认”
    而且 系统不得自动继续联网或修改实例目录

  @M2-INSTALL-004
  场景: 安装提交前不暴露半完成实例
    假如 游戏文件正在任务专用暂存区中准备
    当 安装尚未通过校验并提交
    那么 实例列表中不应出现可启动实例
    而且 已提交的共享文件和其他实例不得被修改

  @M2-INSTALL-005
  场景: 离线时使用已缓存的官方版本目录
    假如 MoyuMax 已缓存一次经过解析的官方版本目录
    当 官方版本服务暂时不可用
    那么 新建实例页应显示缓存的版本目录并标记为离线缓存
    而且 不应阻塞用户管理已有本地实例

  @M2-INSTALL-006
  场景: 用户拒绝恢复异常中断任务
    假如 一个安装任务正在等待恢复确认
    当 用户拒绝继续该任务
    那么 系统应只清理该任务的专用暂存区
    而且 任务历史应标记为已取消
    而且 已提交的共享文件、Java 环境和实例记录不得被删除
