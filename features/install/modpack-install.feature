# language: zh-CN
@M29 @UI-PACK-001
功能: Modrinth 与 CurseForge 整合包安装与更新
  为了直接游玩社区整合包
  作为 MoyuMax 用户
  我希望导入 .mrpack 或 CurseForge .zip 创建实例并安全更新

  场景: 导入 Modrinth 整合包并预览
    假如 用户选择一个 .mrpack 文件
    当 系统解析
    那么 应显示名称、版本、游戏与加载器要求、文件数量与总大小

  场景: 安装整合包创建实例
    假如 用户确认安装一个整合包
    当 安装完成
    那么 游戏与加载器应按包依赖就绪
    而且 全部包文件应经哈希校验后原子进入实例目录

  场景: 安装失败完整回滚
    假如 安装过程中任一文件校验失败
    那么 实例目录应回到安装前状态
    而且 不留半成品文件

  场景: 新版整合包更新实例
    假如 实例由整合包 v1 安装且用户未改动受管文件
    当 用户导入同格式 v2 更新
    那么 v2 新增与变化的文件应替换
    而且 v1 有而 v2 没有的文件应被删除

  场景: 更新保护用户改动
    假如 用户改动过某个受管文件
    当 用新版整合包更新
    那么 该文件应保留不动并出现在提示清单中

  场景: 游戏版本不一致拒绝更新
    假如 新版包要求不同的 Minecraft 版本
    当 用户尝试更新
    那么 系统应拒绝并说明原因

  @UI-PACK-001
  场景: 整合包导入与更新在小窗口与大字体下可用
    假如 窗口为 960×600 且文本放大 200%
    当 用户导入并更新整合包
    那么 不应出现横向溢出、遮挡或文本贴边
