# MoyuMax 项目交接

## 1. 交接检查点

- 交接日期：2026-07-23（M9 完成后更新）。
- 当前分支：`main`。
- 初始 handoff 提交：`cb59361c39d8942831c325c7cfc1ad8616986f8d`。本文件是持续更新的恢复入口，接手时以 `git log -1 -- .continue-here.md` 返回的最新提交为准。
- 远端：`origin`，仓库为 `git@github.com:SakuraRed/MoyuMax.git`。
- M9 已提交为 `0083490`（用户已授权本次提交；未授权推送远端）。
- `main` 已跟踪 `origin/main`；接手时必须再次核对本地与远端 OID。
- 当前版本：`0.1.0-preview.1`，只能称为开发预览，不能称为首个公开预览版。
- 当前平台承诺：Windows 10 22H2 及以上、x64。Windows 11 ARM64 仅通过系统 x64 兼容层尽量兼容，未经实机验证不得标记正式支持。

恢复工作时先执行（本环境没有 RTK，以下为当前可直接使用的命令）：

```bash
git status --short --branch
git log -5 --oneline --decorate
corepack pnpm install --frozen-lockfile
corepack pnpm --filter @moyumax/desktop lint
cmd.exe //c "target\\tmp\\vc.bat cargo test --workspace"
```

Windows 上执行 Cargo 或 Tauri 编译前必须先加载：

```text
C:\BuildTools\VC\Auxiliary\Build\vcvars64.bat
```

M9 期间本机 harness 为 Git Bash + cmd。`target/tmp/vc.bat` 是未跟踪的临时包装（内容：`call vcvars64.bat` 后透传参数），可按需重建；cargo 命令经它调用即可。前一版 harness 要求 `rtk` 前缀，当前环境不存在，不要机械复制旧命令。文件编辑直接用内置编辑工具。

## 2. 权威上下文读取顺序

接手者按下列顺序阅读，避免把 mockup、计划或浏览器模拟误认为真实能力：

1. `AGENTS.md`：项目架构、验证、数据安全和 Git 规则。
2. `docs/UI-UX-REQUIREMENTS.md`：此前产品讨论的统一需求基线，包含范围、页面、交互、性能、隐私、i18n、BDD 和发布门。
3. `docs/architecture/ADR-0001-DESKTOP-STACK.md`：Rust、Tauri、Svelte、SQLite 的架构决策与复核条件。
4. `docs/plans/MILESTONE-01-FIRST-RUN.md` 至 `MILESTONE-10-DOWNLOAD-SOURCES.md`：已执行里程碑边界；`docs/plans/DOWNLOAD-SOURCES-AND-ACCELERATION.md` 的剩余部分（全局限速、设置界面）随后续里程碑执行。
5. `features/`：行为规格；新增行为必须先在这里定义示例。
6. `.codex/evals/`：验证证据及 implemented、validated 的边界。
7. `README.md`：面向贡献者的当前摘要，不应覆盖以上详细规范。

视觉实现必须对照本机 mockups：`D:/Downloads/MoyuMax-mockups`。mockups 是设计输入，不是已实现能力证明。用户负责后续 mockups，核心开发、测试、打包和工程判断交由接手 agent；零星低影响问题自行按现有规范决定，不要恢复长轮次逐项追问。

若文档出现冲突，优先顺序为：用户最新明确决定、`AGENTS.md` 安全边界、UI/UX 需求书、已验证 Gherkin 与评估证据、里程碑计划、mockups。不得因已有 UI 占位就宣称后台能力完成。

## 3. 产品定位和不可变承诺

MoyuMax 是面向 Minecraft Java Edition 的开源、免费、安装式统一管理应用，不是单文件启动器。核心差异是由常驻后台核心、数据库和受管存储提供环境部署、游戏安装、内容管理、数据恢复与诊断的一站式体验。

必须长期保持：

- 普通用户不需要自行理解或安装 Java、加载器、依赖与启动参数。
- 启动器本身快速显示、快速唤醒、低占用；游戏自动调优是后续愿景，不得牺牲启动器性能。
- 本地基础功能始终完整离线可用；未来云服务可以收费，但不得锁住本地数据或基础功能。
- 默认不上报遥测或崩溃，不默认联网执行敏感操作，不默认开放公网监听。
- 不擅自安装模组，不擅自修改用户配置。
- 实例默认完全隔离；用户关闭时必须提示实例互相干扰风险，但充分告知后允许继续。
- 安装、更新、迁移、删除和 CLI 写操作必须事务化、可恢复或有可验证补偿路径。
- GUI、CLI、托盘与后台任务最终共用同一核心状态和事务模型。
- 客户端许可证为 `GPL-3.0-only`，依赖必须兼容。

## 4. 已确认产品决策

### 4.1 平台和应用生命周期

- 当前只开发 Windows，首发 Windows 10 22H2+ x64。
- 使用传统每用户 EXE 安装器，不提权。
- 目标是托盘常驻。首次关闭窗口询问“退出”或“最小化到托盘”，允许记住选择；托盘右键必须有退出，并提供显示、最近实例、任务摘要和暂停任务。
- 启动器检查更新可以提示，但下载和安装必须由用户手动触发；自动下载和安装默认不存在或关闭。
- 快速唤醒是硬要求，不能让用户感知明显启动延迟。
- macOS/Linux 只预留平台边界。Linux 可用 WSL 验证核心，不能据此宣称 Linux 桌面支持；macOS 后续用用户实机验证。

### 4.2 技术架构

- Rust：与 UI 无关的本地领域、SQLite、事务、下载、任务与未来 CLI 核心。
- Tauri 2：桌面生命周期、IPC 和 Windows 集成适配层。
- Svelte 5 + TypeScript：编译型声明式 UI，禁止用命令式 DOM 造成布局状态漂移。
- SQLite：本地权威状态库，当前 `PRAGMA user_version = 15`。
- `crates/moyumax-core` 不依赖 Tauri；`apps/desktop/src-tauri` 不承载业务规则；`apps/desktop/src` 不直接写数据库或受管目录。
- 正式桌面构建必须调用 Rust 核心；浏览器 runtime 只服务确定性 UI 测试。

### 4.3 Java、实例和共享存储

- 当前托管 JDK 使用 Azul Zulu。此前提出过“研究最优 Minecraft JDK”，但仓库没有可复现对比基准，因此只能说 Zulu 是当前兼容基线，不能宣称性能最优。
- 安装游戏时检测并随任务安装所需 JDK，保存在 MoyuMax 受管数据位置，不修改系统 Java。
- Java 唯一身份为“发行版、完整补丁构建、架构”，相同构建全局只存一份。
- 删除实例或最后一个 Java 引用不会自动删除 JDK；只允许在环境管理中手动删除。仍被引用时提示风险，用户坚持后允许删除。
- 多实例共用可校验重建的 Minecraft 基础文件；实例可变数据默认隔离。
- 数据索引以数据库为准，不依赖硬链接或符号链接。数据迁移必须复制、校验、原子切换数据库索引，再删除原文件。
- 数据默认位于应用管理位置，可迁移到本地固定磁盘或 USB。SMB/NAS 不作为托管实例位置。

### 4.4 下载、来源和任务

- 所有下载进入统一持久化任务队列，自动控制并发；当前实现一个共享后台任务槽、全局暂停/恢复（M9）与单任务暂停、优先级、全局限速、网络压力感知并发（M14）。
- 支持续传与校验；来源无法续传时只清理对应未完成内容并重下。
- 没有可信校验值时弹窗说明风险；用户拒绝则清理暂存，用户坚持则允许并记录决定。
- 来源默认“内置镜像优先”；用户可切换为“官方源优先”或自定义源。
- Modrinth 与 CurseForge 的内置模组镜像使用 MCI Mirror API：`https://www.mcimirror.top/`。Modrinth 保留官方源，并可与镜像按策略和健康状态互切；CurseForge 官方源不可用，选择官方源时必须明确提示并只提供 MCI Mirror 路径。
- Minecraft 游戏文件默认使用按 `https://bmclapidoc.bangbang93.com/` 文档接入的 BMCLAPI 镜像，同时保留 Mojang 官方源。文档 URL 不是可直接猜测的文件下载基址。
- 自定义源绝不切换到其他来源，设置中持续说明并提供 DNS、连接、TLS、状态和延迟测试。
- 下载速度优化纳入 P0：跨文件并发之外，大文件在来源可靠支持 HTTP Range 时使用有界多线程分段下载；动态调整分段数，失败时安全降级单连接，最终完整哈希和原子提交不变。
- 当前 M3 实现跨文件受控并发与单连接续传，M10 已实现 MCI Mirror、BMCLAPI 路由与单文件多线程分段；CurseForge 仍仅官方不可用语义。
- 模组自动更新默认关闭并按实例管理；任何更新策略变化都需提示影响范围。

### 4.5 账户和凭据

- 首个公开预览目标包括 Microsoft、离线、Authlib Injector、LittleSkin、统一通行证。
- Microsoft 项目名义应用注册完成前不得标记公开预览。
- 默认只保存令牌。可选在本地加密保存第三方账户密码，并明确设备入侵和供应链风险。
- 密码保险库使用独立主密码；主密码只保护查看明文和迁移导出，日常使用可跨重启保持可用。
- 忘记主密码不恢复，只清除已保存密码。
- 来源格式允许时，后续迁移可复制现有令牌。
- 当前离线与 Authlib Injector（含 LittleSkin 快捷选项）账户已实现（M20）；Microsoft 登录受应用注册进度限制，仅有文字说明；统一通行证预设地址后续补。

### 4.6 模组生态和整合包

- 首版在线目录仍以 Modrinth 为主；MCI Mirror 作为 Modrinth 与 CurseForge 的默认文件镜像。CurseForge 可用目录能力以 MCI Mirror 实际 API 为准，不接入 CurseForge 官方下载源，也不得猜测未公开端点。
- 当前已完成 Fabric、Quilt、Forge、NeoForge 的模组搜索、必需依赖解析、原子安装，以及按实例更新策略（M15）；更新恢复点为事务级快照，成功或回滚后即清理。
- 不执行整合包脚本或任意代码。MoyuMax 原生格式必须公开、版本化、严格声明式。
- 导入冲突集中汇总，支持逐项或批量选择。
- 导出默认使用下载清单；只有许可证证据明确允许时才嵌入模组文件。
- 第二个公开版本再做其他启动器迁移；迁移复制到 MoyuMax 受管仓库，始终保留来源目录。
- 首个公开预览仍需要 Forge、NeoForge、Quilt，以及资源包、光影、数据包、世界、截图和更完整的内容更新策略。

### 4.7 备份、回收站和诊断

- 默认在游戏启动前与退出后备份世界。全量快照使用完整原子 ZIP；游戏运行期间按可配置间隔（默认 30 分钟）创建只含差异的增量备份（M19），保留数量可配置（默认 20），回滚可落到任一增量点。
- 未来支持游戏运行期间定时增量备份、可配置间隔和数量、世界时间线与回滚；回滚前先创建恢复点。
- 内置回收站默认保留 30 天。当前仅保存到期元数据和手动永久删除，不会自动清理到期内容。
- 删除实例不删除 Java、共享基础文件、账户或诊断导出。
- 卸载时未来允许分别保留实例、存档、备份、账户数据与 JDK，默认建议保留个人数据。
- 崩溃报告完全本地，包含游戏末尾输出、游戏日志、启动器日志、环境、脱敏启动命令与脚本等证据；导出前预览清单和脱敏摘要，绝不自动上传。

### 4.8 网络和联机

- 只管理客户端。首版不直接实现 P2P 建房或服务端管理。
- 规划检测实例局域网端口并转发到用户指定的本机端口。
- 端口转发默认关闭；开启公网绑定后允许公网访问，但 MoyuMax 不负责路由器映射或防火墙配置。
- NAT 类型检测只在用户手动触发并知情后访问 STUN。
- 以上端口转发和 NAT 功能当前均未实现。

### 4.9 UI、主题、i18n 和安全边界

- UI 必须声明式；所有卡片、弹窗、行和框体中的文本必须有安全内边距，不允许贴边、重叠或依赖横向滚动完成主任务。
- 最低窗口 960×600，关键页面必须覆盖 200% 文本缩放。
- 主题支持跟随系统、浅色、深色、自定义背景、视频或动画，以及第三方纯数据主题包。
- 主题包不能提供任意 CSS、脚本、布局或安全控件覆盖；主题和语言包禁止引用远程图片、字体或视频。
- 内置简体中文、繁体中文和英文；社区语言包只能是有版本与完整性信息的纯数据包，不能覆盖安全关键文案。
- 动态背景必须在窗口隐藏、游戏运行、节能、减少动画或资源超限时暂停并释放资源。
- 当前 UI 只有简体中文开发文案和单一视觉基线，完整主题、三语和无障碍发布验收仍未完成。

### 4.10 CLI、云与商业模式

- 未来内置 CLI，在开发者设置中显式开启并提示风险。
- CLI 与 GUI 共用核心，写操作支持 `dry-run`、版本化 JSON、稳定退出码和进度事件；不得读取凭据、永久删除数据或修改安全设置。
- 云同步仅预留接口，首版保持单机。未来使用端到端加密，服务端无法恢复数据，用户通过助记词恢复。
- MoyuMax 开源、免费；后续可以对可选云服务收费，但不对基础功能收费，且必须完整离线可用。
- 用户曾提出公开协议但保密部分加密技术细节。该要求与 GPL 客户端的可审计实现可能存在冲突，进入云同步设计前必须形成单独 ADR 和法律/威胁模型结论，不能直接假设可以闭源客户端加密实现。

## 5. 当前代码结构

核心模块：

- `crates/moyumax-core/src/lib.rs`：服务入口、数据库初始化与 schema v15。
- `accounts.rs`（M20 新增）：离线与 Authlib Injector 账户模型、Yggdrasil 认证客户端、默认账户、会话刷新与启动身份解析。
- `catalog.rs`：Minecraft、Fabric、Quilt、Forge、NeoForge、Azul 元数据和缓存。
- `loader_install.rs`（M12 新增）：spec-1 安装器 profile 解析、Maven 坐标、占位符展开、处理器计划与托管 Java 执行编排。
- `install.rs`：安装计划和持久化任务。
- `execution.rs`：下载、续传、校验、解包、原子发布、下载暂停中断（`DownloadInterrupt`）、来源候选回退（`fetch_with_policy`）、Range 分段并行下载、全局令牌桶限速（`RateLimiter`）、压力感知并发（`AdaptiveConcurrency`）与内容更新快照替换/回滚（M15）。
- `launch.rs`：参数展开、完整性校验、进程会话与日志。
- `content.rs`：Modrinth 搜索、依赖、内容事务、全加载器内容安装、按实例更新检查/计划与中断恢复（M15）。
- `diagnostics.rs`：崩溃报告、脱敏和本地 ZIP 导出。
- `recycle.rs`：实例回收、恢复、永久删除与中断收敛；M18 扩展为统一删除事务（截图/资源/世界共用 intent→rename→finalize 与 payload 无损恢复）。
- `resources.rs`（M16 新增）：实例资源内容（资源包/光影/数据包）导入事务、`.disabled` 启用切换、世界只读列举与实例隔离。
- `worlds.rs`（M17 新增）：世界清单、ZIP 导入导出、备份回滚（恢复点 + saves 原子交换）与中断收敛。
- `screenshots.rs`（M18 新增）：实例截图清单、读取与位置解析。
- `backup.rs`：会话前后世界快照、运行期间定时增量备份（M19）、链感知保留策略与中断恢复、定时调度循环。
- `shell.rs`（M9 新增）：关闭窗口行为、壳层页面/滚动状态、全局任务暂停标志、退出影响汇总、最近实例与下载限速设置。
- `java_env.rs`（M13 新增）：托管 Java 环境清单、删除墓碑、恢复、实例指派与中断收敛。
- `source.rs`（M10 新增）：下载来源策略（镜像优先/官方优先/自定义）、BMCLAPI 与 MCI Mirror 路由表、CurseForge 官方不可用语义与候选生成。

桌面层：

- `apps/desktop/src-tauri/src/lib.rs`：Tauri 命令、任务/启动协调器、窗口关闭拦截与退出事件接线。
- `apps/desktop/src-tauri/src/lifecycle.rs`（M9 新增）：窗口隐藏/销毁/重建、空闲销毁、优雅退出编排与 `MOYUMAX_SMOKE` 采样驱动。
- `apps/desktop/src-tauri/src/tray.rs`（M9 新增）：托盘图标、菜单模型（纯数据可测）、菜单事件路由与 3 秒事实重建。
- `apps/desktop/src/runtime.ts`：版本化前端 runtime 接口及浏览器测试实现。
- `apps/desktop/src/close-flow.ts`、`shell-state.ts`（M9 新增）：关闭决策状态机与唤醒恢复白名单/敏感回退。
- `apps/desktop/src/components/CloseDialog.svelte`（M9 新增）：首次关闭选择与退出影响确认对话框。
- `apps/desktop/src/components/`：Svelte 声明式页面。
- `apps/desktop/tests/`：Playwright 用户行为和缩放/间距验收。
- `scripts/release-smoke.mjs`（M9 新增）：Release 唤醒与后台内存冒烟驱动，输出到 `output/release-smoke/`。

安装器：

- `apps/desktop/src-tauri/tauri.conf.json` 只启用 NSIS，`installMode = currentUser`。
- 安装器契约由 `apps/desktop/src-tauri/tests/installer_bundle_bdd.rs` 固定。
- 仍缺正式图标、Authenticode、升级/卸载数据选择和完整托盘生命周期。

待实现下载架构见 `docs/plans/DOWNLOAD-SOURCES-AND-ACCELERATION.md`。它不会改变当前 M3/M5 已验证事实，后续迁移必须先完成镜像契约测试和来源回归。

## 6. 已完成并验证的里程碑

| 里程碑 | 提交 | 已验证结果 |
|---|---|---|
| M1 首次运行 | `8befabf` | 离线首次引导、SQLite 事务、首页空状态、缩放 |
| M2 安装计划 | `197d231` | 官方元数据、Fabric、Zulu 选择、Java 去重、持久化计划与恢复确认 |
| M3 安装执行 | `78b6c2a` | 续传、校验、安全解包、共享缓存、原子实例提交 |
| UI 框体间距 | `df844cc` | 声明式安全内边距和缩放回归 |
| M4 游戏启动 | `605355b` | 托管 Java、Mojang/Fabric 参数、进程会话、停止、日志、真实启动探针 |
| M5 Modrinth | `3983143` | 搜索、依赖闭包、双哈希、文件与数据库补偿事务、真实 Continuity 探针 |
| M6 崩溃诊断 | `969da28` | 本地报告、规则、脱敏、预览后原子 ZIP 导出 |
| M7 回收站 | `8a331d8` | 实例回收、恢复、永久删除、中断收敛、Java 保留 |
| M8 世界备份 | `a0a037c` | 启动前/退出后原子 ZIP、无世界跳过、失败阻断、保留 20、中断恢复、UI 时间线 |
| Windows 安装器 | `9f4b45b` | x64 NSIS、每用户安装模式、开发预览标识 |
| M9 托盘生命周期 | `0083490` | 关闭选择记忆、托盘菜单统一事实、隐藏+空闲销毁快速唤醒、全局暂停恢复、退出保护、Release 冒烟 |
| M10 下载来源与分段加速 | `6f0bfbd` | 来源策略引擎、MCI/BMCLAPI 路由、官方回退记录、Range 分段下载与降级、任务来源详情、基准 2.95× |
| M11 Quilt 加载器 | `e362926` | Quilt 版本列表与稳定推荐、profile SHA-256 校验、安装原子提交、Quilt mainClass 启动 |
| M12 Forge/NeoForge 处理器 | `862f2f1` | spec-1 安装器解析、客户端处理器链执行、PATCHED 校验入库、双加载器启动 |
| M13 Java 环境管理 | `45cc44a` | 环境清单与引用、引用删除确认、墓碑保留与重启收敛、一键恢复、设环境主版本校验 |
| M14 任务控制 | `3803df9` | 单任务暂停/恢复、优先级、全局令牌桶限速、网络压力感知并发收缩 |
| M15 全加载器与内容更新 | `a9d80fa` | Quilt/Forge/NeoForge 模组安装、按实例更新检查与快照替换回滚、中断恢复旧文件、自动更新开关持久化 |
| M16 实例资源内容管理 | `7d52781` | 资源包/光影/数据包本地导入事务、同名拒绝、`.disabled` 启停、数据包世界选择、实例隔离 |
| M17 世界存档管理 | `89f84c6` | 世界清单、ZIP 导入导出、回滚恢复点与 saves 原子交换、中断重启收敛 |
| M18 截图与统一删除回收站 | `5103254` | 截图清单/筛选/剪贴板复制、截图/资源/世界删除统一回收站事务、payload 无损恢复、全类型永久删除 |
| M19 运行期间增量备份 | `922b31e` | 文件级增量与删除清单、链式回滚、链感知保留、间隔/保留配置、会话内定时调度 |
| M20 账户入口 | 未提交 | 离线账户、Authlib/LittleSkin 登录、默认账户、会话刷新与过期阻断、启动身份解析 |


“validated”仅适用于上述已记录的范围，不代表整个首个公开预览完成。

## 7. 最近一次验证证据

2026-07-23（M20）在 Windows x64 本机执行：

- `cargo test --workspace`：157 个非忽略测试 PASS（含 `accounts_bdd` 7/7）；5 个 ignored 保持不变。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`、`cargo fmt --all -- --check`：PASS。
- `pnpm --filter @moyumax/desktop lint`：Svelte 0 错误、0 警告。
- `pnpm --filter @moyumax/desktop test`：Vitest 16/16 PASS。
- `pnpm --filter @moyumax/desktop test:e2e`：Playwright 78/78 PASS（accounts 6/6；本机 1420 端口被 HYP.exe 占用，同等配置改用 1421 复跑）。
- `pnpm --filter @moyumax/desktop build`、`pnpm --filter @moyumax/desktop tauri build`（x64 NSIS，未签名）、`git diff --check`：PASS。
- 范围说明：Microsoft 登录未实现（应用注册外部阻塞），仅有文字说明；真实 LittleSkin 探针留待发布候选；schema 升至 v15 自动迁移。

M19 证据（同日）：Rust 150、Playwright 72/72，详见 `.codex/evals/milestone-19-incremental-backup.md`。

最新详细证据见 `.codex/evals/milestone-20-accounts.md`。最近一次全量测试没有运行 ignored live tests；其历史真实探针结果记录在各里程碑文档，但外部版本会漂移，需要在发布候选时重新执行。

## 8. 首个公开预览尚未完成

P0 发布闭环缺口：

1. ~~托盘常驻、关闭选择记忆、UI 销毁后的轻量后台、快速唤醒和完整退出保护~~（M9 已 validated）。
2. Microsoft、Authlib Injector、LittleSkin、统一通行证正式账户入口；~~Authlib Injector / LittleSkin~~（M20 已 validated，含离线账户与启动身份解析）；统一通行证预设地址与 Microsoft 登录仍未实现，Microsoft 项目应用注册仍缺（外部阻塞）。
3. ~~Forge、NeoForge、Quilt 安装与启动~~（M11 Quilt、M12 Forge/NeoForge 已 validated；真实处理器 live 探针留待发布候选执行）。
4. ~~完整任务暂停、恢复、优先级、限速和网络压力感知并发~~（M14 已 validated；磁盘/CPU 压力感知与计划时段下载属后续）。
5. ~~默认镜像优先来源策略与多线程分段下载~~（M10 已 validated；设置页来源界面未实现，CurseForge 仅官方不可用语义，全局限速未实现）。
6. ~~Java 环境管理界面和删除/恢复闭环~~（M13 已 validated；设置页其余分组随各自里程碑进入）。
7. ~~Modrinth 按实例更新策略~~（M15）；~~资源包、光影、数据包管理~~（M16）；~~世界存档管理~~（M17）；~~截图管理与删除回收站统一~~（M18）。缺口 #7 全部关闭。
8. ~~运行期间定时增量备份、可配置保留数量、世界历史和回滚恢复点~~（M19 已 validated；差异检测为大小+mtime 的 rsync 语义）。
9. 简体中文、繁体中文、英文完整 i18n；系统/浅色/深色、自定义背景和纯数据主题包。
10. 键盘、屏幕阅读器、高对比度、减少动画和目标基准设备完整验收。
11. 内置 CLI 及 GUI/CLI 共用写事务。
12. 更新提示、手动更新、失败回滚、降级快照和数据兼容检查。
13. 卸载数据保留向导、正式图标、SBOM、第三方许可清单、Authenticode 和来源证明（M9 使用开发占位托盘图标）。
14. GitHub Actions Windows CI、自动测试、构建和发布候选验证。目前仓库没有 `.github/workflows`。
15. 5 名 Windows 新手的发布形成性测试及第 12 节正式性能基准（M9 已给出唤醒与后台内存样本，冷启动与前台内存正式基准未测）。

第二个公开版本及以后：其他启动器迁移、完整整合包格式矩阵、macOS/Linux、云同步、P2P、服务端和扩展加载器。CurseForge 更广泛的搜索和元数据能力取决于 MCI Mirror 可验证 API，不得用未实现占位制造可用假象。

## 9. 推荐的下一里程碑

账户入口主体已闭环（离线 + Authlib/LittleSkin，缺口 #2 的剩余项只有 Microsoft 注册外部阻塞与统一通行证预设）。建议下一里程碑执行 i18n 与主题基础（缺口 #9 前半）：简体中文、繁体中文、英文三语界面文案外置与切换，系统/浅色/深色主题。开始编码前先写 Gherkin 和 eval；文案外置必须一次到位，不允许界面继续硬编码新文案。

随后顺序：无障碍验收（缺口 #10）→ CLI（缺口 #11）→ 更新与卸载闭环（缺口 #12、#13）→ CI 与发布验收（缺口 #14、#15）；Microsoft 注册完成后补正式登录。若用户另行指定优先级，以用户最新要求为准。

## 10. 开发和提交规则

- 行为变更先写 `features/` Gherkin，再写失败测试，执行 Red-Green-Refactor。
- 受管数据写入必须有事务、原子提交或可验证补偿。
- 前端只能通过版本化 runtime 调用核心，不直接碰 SQLite 或受管目录。
- Node 使用 `pnpm@10.26.2`，依赖精确版本并提交 lockfile；禁止 `^`、`~`、`latest` 或无界范围。
- 不提交 `target/`、`dist/`、数据库、运行数据、凭据、诊断导出、安装包或截图临时文件。
- 所有完成声明附命令、测试、运行、视觉、Git 或发布证据。
- Git 身份：`SakuraRed <86900315+SakuraRed@users.noreply.github.com>`。
- 提交必须使用 `C:/Users/Administrator/.ssh/id_ed25519.pub` 的 SSH 签名并包含 DCO `Signed-off-by`。
- 推送、发布、创建标签或修改远端工作流前需要用户明确授权；本次用户只明确授权推送当前 handoff 到 Git，没有授权发布安装包或创建 Release。

完整验证门（当前环境可直接执行）：

```bash
corepack pnpm --filter @moyumax/desktop lint
corepack pnpm --filter @moyumax/desktop test
corepack pnpm --filter @moyumax/desktop test:e2e
corepack pnpm --filter @moyumax/desktop build
cmd.exe //c "target\\tmp\\vc.bat cargo fmt --all -- --check"
cmd.exe //c "target\\tmp\\vc.bat cargo test --workspace"
cmd.exe //c "target\\tmp\\vc.bat cargo clippy --workspace --all-targets --all-features -- -D warnings"
git diff --check
```

NSIS 构建与 Release 冒烟：

```bash
corepack pnpm --filter @moyumax/desktop tauri build
node scripts/release-smoke.mjs --cycles 10
```

注意：直接 `cargo build --release` 产出的 exe 会指向 devUrl（缺少 Tauri CLI 注入的上下文），渲染“127.0.0.1 拒绝连接”；性能或界面验证必须使用 `pnpm tauri build`（或 `tauri build --no-bundle`）的产物。

## 11. 接手前检查清单

- [ ] `git status` 核对 M9 未提交文件清单（第 6 节），`main` 与 `origin/main` OID 一致。
- [ ] 已完整阅读本文件、`AGENTS.md`、UI/UX 需求书和 ADR。
- [ ] 没有把 browser runtime 的模拟结果当成 Rust 核心实现。
- [ ] 已确认下一个里程碑（M10 下载来源）的 Gherkin、eval、主路径、失败路径、恢复路径和性能风险。
- [ ] 已对照 mockups 检查所有框体文本内边距、960×600 和 200% 缩放。
- [ ] 没有启用遥测、自动更新、公网监听、远程主题资源或任意脚本。
- [ ] 没有声称 unsigned 开发构建为公开预览或正式发行版。

恢复后从第 9 节 M10 建议开始；如果用户另行指定优先级，以用户最新要求为准，并同步更新本交接文件或新的里程碑计划。
