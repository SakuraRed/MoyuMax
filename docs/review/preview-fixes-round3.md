# 预览版问题修复复审清单（2026-07-24，第三轮：UIUX 重构）

构建：`D:\downloads\MoyuMax_0.1.0-preview.1_x64-setup.exe`（自签名，新图标）。
针对第三轮 6 个问题逐项复核；[自动] 为自动化已覆盖，[人工] 请实际操作确认。

## 1. UIUX 重构（排版/字体/视觉）

- [自动] 滚动条全局可见（细条悬停加深）；首页 hero 主卡（大封面、默认账户行）；
  资源卡含图标/作者/版本徽章/mono 下载量；三语键一致。
- [人工] 浏览各页面（深色与浅色各一遍），检查文字错位、卡片对齐、间距异常；
  重点看：首页、资源在线目录、新建实例、设置账户区。

## 2. 资源目录 HMCL 化

- [自动] 打开资源页默认显示热门（下载最多排序，无需输入搜索词）；筛选行：
  目标实例/游戏版本/加载器/排序；结果卡有图标（无图标显示首字母）、作者、
  版本徽章、下载量与更新时间；底部「加载更多（剩余 N 条）」。
- [人工] 联网打开资源页：确认真实热门列表自动出现（Sodium/Iris 等应在前列）；
  切换「整合包/光影/资源包」芯片确认默认热门同样加载；改游戏版本与加载器
  筛选后列表刷新；拉到底部点「加载更多」确认分页追加。

## 3. 整合包下载中断（gzip）

- 根因：CDN 以 gzip 内容编码返回，客户端未启用解压特性，已修复并加回归测试
  （gzip 编码 fixture 必须解码后校验 SHA-1）。
- [人工] 在线目录 → 整合包芯片 → 任选一个真实整合包（如 Adrenaline）→
  安装 → 预览出现 → 确认安装 → 新实例创建成功。

## 4. 滚动

- 实测滚动容器约束正常；已补全局可见滚动条便于感知。
- [人工] 在各页面长内容上滚动（资源目录列表、新建实例版本组、设置页），
  确认滚轮/触摸板滚动顺畅且滚动条可见。

## 5. Microsoft 登录（新 Client ID）

- 根因：旧注册（a5897d46…）帐户类型不含个人 MSA（AADSTS700016）。
  已接入新 Client ID `7f00149b-8b23-4ac4-86ef-d982a58c07a3`，端点改 common
  （组织与个人通吃）。
- [人工] 设置 → 账户 → 添加 Microsoft 账户：设备码面板出现 → 浏览器输入码
  登录 → 账户入库成功。若再失败，错误条含 AADSTS 详情，请原文发我。
- [人工] 登录成功后：刷新会话、设为默认、启动实例进正版服务器。

## 6. 版本选择分级折叠

- [自动] 新建实例版本区：正式版按大版本折叠组（Minecraft 1.21 ▾ 2 个版本），
  推荐稳定版所在组默认展开并选中；「快照与测试版」「远古版本」独立折叠组。
- [人工] 展开/收起各组，选 1.20.1 或快照，确认加载器列表随之刷新；
  展开远古版本选择 b1.7.3 确认可选（无需真的安装）。

## 回归基线

- Rust 195/195、Vitest 19/19、Playwright 119/119、clippy/fmt 零告警、
  SBOM 607 依赖一致（gzip/deflate 特性新增 miniz_oxide 等）。

## 发现问题记录

| # | 条目 | 现象 | 截图/日志位置 | 严重度 |
|---|------|------|---------------|--------|
|   |      |      |               |        |
