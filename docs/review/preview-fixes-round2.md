# 预览版问题修复复审清单（2026-07-24，第二轮）

构建：`D:\downloads\MoyuMax_0.1.0-preview.1_x64-setup.exe`（自签名开发证书，新图标）。
针对你报告的 7 个问题逐项复核；自动化已覆盖的标注 [自动]，其余请人工确认。

## 1. Microsoft 设备码 HTTP 400

- 代码已改为透传服务端错误详情（`AADSTS…` 描述会直接显示在错误条里）。
- [人工] 请按下表检查 Azure 应用注册后重试登录：
  1. Azure 门户 → 应用注册 → 选 `a5897d46-0863-48dd-84f2-467896967591`
  2. **身份验证 → 高级设置 → 设备代码流**：必须是「是」（这与「允许公共客户端流」
     是两个开关，400 最常见原因就是它没开）
  3. 「允许公共客户端流」保持「是」
  4. 支持的帐户类型须包含「个人 Microsoft 帐户」
- [人工] 重试登录：若再失败，错误条会显示具体 AADSTS 描述，把它发给我。
- [自动] 设备码错误详情透传（msauth_bdd 11/11）。

## 2. 子页面侧边栏不可用

- [自动] 所有页面侧边栏统一接线（navigation.spec 2/2），各页面顶部冗余的
  「返回首页」按钮已移除（侧边栏承担导航）。
- [人工] 抽查：进入「实例（新建）/资源/任务/数据/设置」后，侧边栏六项均可
  直接互跳，不再需要先返回首页。

## 3. 资源页改为在线目录

- [自动] 资源页默认「在线目录」：模组/整合包/光影/资源包四类芯片 + Modrinth
  搜索；「实例内容」标签保留本地管理（online-catalog 5/5、content-install、
  content-updates、instance-resources 全绿）。
- [人工] 联网搜索真实关键词，确认四类结果真实返回；模组走「查看安装计划」，
  整合包走「安装 → 预览 → 确认安装」，光影/资源包直接安装到所选实例。
- 说明：在线目录仅 Modrinth（MCI Mirror 无搜索 API）；CurseForge 整合包请用
  整合包芯片下的「导入本地整合包」（跳转新建实例页顶部导入区）。

## 4. 数据页无实例打不开

- [自动] 根因是侧边栏未接线，已修；空实例数据页各区空态正常（截图复核）。

## 5. 导入整合包入口

- [自动] 首页空状态「导入整合包」已启用并跳转新建实例页导入区；
  整合包更新入口保留在实例卡。
- [人工] 用一个真实 .mrpack 和一个 CurseForge .zip 各导入一次。

## 6. 图标重做

- [人工] 安装后看任务栏/开始菜单图标：深底圆角 + 等距绿色立方体 +
  顶面像素 M（16/32/256 全尺寸程序化重绘）。
- 不满意请描述期望方向，图标是纯代码生成，可快速迭代。

## 7. 界面设计语言清理

- [自动] 删除/精简 30+ 处机制说明文案（队列原理、哈希承诺、里程碑注记等），
  三语 677 键一致（i18n 测试通过）。
- [人工] 浏览各页面，如仍看到「写给开发者」的说明文字，截图指出位置。

## 回归基线

- Rust 194/194、Vitest 19/19、Playwright 119/119、clippy/fmt 零告警、
  SBOM 604 依赖一致。

## 发现问题记录

| # | 条目 | 现象 | 截图/日志位置 | 严重度 |
|---|------|------|---------------|--------|
|   |      |      |               |        |
